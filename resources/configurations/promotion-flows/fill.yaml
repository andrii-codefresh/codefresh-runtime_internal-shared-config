apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: fill
  annotations:
    description: ""
spec:
  triggerEnvironment: dev
  steps:
    - environment: qa
      dependsOn:
        - dev
      policy:
        action: commit
